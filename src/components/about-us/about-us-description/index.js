import { El } from '@/components/shared/El';

export function AboutUsDescription() {
  return El({
    element: 'div',
    className: 'bg-blue-400 text-white',
    innerText: 'about us description',
  });
}
